#include <bits/stdc++.h>
#include "head.hpp"

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int a = 2, b = 5;
    std::cout << sub(a, b) << std::endl;
    std::cout << mult(a, b) << std::endl;

    return 0;
}